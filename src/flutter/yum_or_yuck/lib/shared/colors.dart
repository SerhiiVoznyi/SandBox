import 'package:flutter/material.dart';

class AppColors {
  // Core
  static const primary = Color(0xFFFF9800); // orange
  static const background = Color(0xFF1F1F23);
  static const surface = Color(0xFF2A2A2E);

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFB0B0B0);

  // Game feedback
  static const yum = Color(0xFF4CAF50);
  static const yuck = Color(0xFFF44336);
}
